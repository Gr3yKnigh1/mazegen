# Maze Generator

## Requirements

* `python>=3.8`

## Usage

1. Install requirements

```shell
pip install -r requirements.txt
```

2. Run

```
python src/main.py
```

## Keybindings

* `<lctrl> + r` - regenerate
* `<lctrl> + q` - quit
